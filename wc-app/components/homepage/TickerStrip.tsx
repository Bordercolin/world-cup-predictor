export function TickerStrip() {
  return (
    <section className="border-y border-ink/10 bg-ink py-5 text-cream">
      <div className="flex overflow-hidden whitespace-nowrap">
        <div className="animate-[ticker_28s_linear_infinite] font-mono text-sm font-black uppercase tracking-eyebrow text-sand">
          Private groups / score predictions / kickoff locks / host-city nights / live rankings /
          coast-to-coast bragging rights / private groups / score predictions / kickoff locks /
          host-city nights / live rankings /
        </div>
      </div>
    </section>
  );
}
